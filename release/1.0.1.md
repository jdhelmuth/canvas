# Canvas Slideshow 1.0.1 (62)

## What's new

- More dependable unattended slideshows, including recovery from unavailable photos.
- Safer Google-to-Apple album mirroring and fresher photo caches.
- Clearer weather freshness and separation of Ambient station readings from weather near the iPad.
- Faster navigation for large photo collections and reduced imported-image memory use.
- More reliable audio playback, imports, date filters, and settings recovery.

## Release preparation

- Reliability implementation: `bbf6225`.
- Live preflight on September 15, 2026: public version 1.0 (61), no pending replacement; version 1.0.1 created in `PREPARE_FOR_SUBMISSION`.
- App Store Connect API confirmed 61 as the latest uploaded build. Xcode Cloud's visible Next Build Number is 62. Source marketing/build versions are 1.0.1 (62).
- Manual Public Release is enabled, uses stable Latest Release Xcode 27 (27A266a) and macOS 27 (26A428), archives for App Store Connect, and assigns the archive to the existing Internal TestFlight group.
- The public workflow now has ASC_APP_ID and secret ASC_ISSUER_ID, ASC_KEY_ID, and ASC_PRIVATE_KEY. Its empty automatic branch condition was removed to satisfy Apple's workflow validation; the workflow remains manual-only. The cloud archive passed the fail-closed preflight.
- Original validation passed 219 unit tests, 14 UI tests, 19 tooling tests, and an unsigned Release build. A later release rerun encountered a slider lookup failure and a stalled simulator runner; the exact slider test passed after a simulator restart without source changes. The final full gate then passed: 219 unit tests, 14 UI tests, and 19 tooling tests, with the two opt-in UI checks skipped.
- Physical iPadOS 27 validation passed on the connected iPad Pro with development build 1.0.1 (62). The opt-in test now expands and scrolls the settings form correctly in landscape, finds the status across accessibility element types, waits for live data, and restores the original weather toggle on failure as well as success. This validates the device's configured provider, not every provider/account combination. Existing preferences were backed up and their decoded settings content remained unchanged. No fresh install or deletion of personal app data was performed.

## Cloud release evidence

- Archive source: `87df67a567d024460e9f66c81dab4c97841a9cc7`.
- Manual Public Release run 62: `bf0f720c-2623-4726-922d-ad8a47a42e17`, `COMPLETE` / `SUCCEEDED`.
- Processed build: `b312a47d-8505-470a-bfb6-a5715f6921ea`, `VALID` / `APP_STORE_ELIGIBLE`, version 1.0.1 (62).
- Internal TestFlight: `IN_BETA_TESTING`; exact build membership verified in group `e4cea94c-30af-456c-94df-862210490408`. English testing notes are saved.
- App Store version `2319346e-595e-4d5f-bb87-81b6eae3ee14` selects that same build and remains `PREPARE_FOR_SUBMISSION` pending the required TestFlight smoke results.
- Subsequent changes affect only UI tests, local-check configuration, and documentation; the archived production source is unchanged.
- Follow-up validation on September 16 passed the complete local gate again: 219 unit tests, 14 UI tests, and 19 tooling tests, with the two opt-in UI checks skipped. Settings and Google-import tests now scroll the actual form and bring offscreen controls into view; weather checks use short drags from a known position. Local checks disable optional verbose system-diagnostic collection, which had stalled after failures; test results, normal attachments, and failure exit status are retained. Simulator startup recovered without erasing the original device or disturbing other projects. The temporary recovery simulator was removed.

## Outstanding release gates

- Complete the TestFlight fresh-install and upgrade smoke checks in RELEASE.md before App Review submission.
- Integrate with the documented CI-skip marker, verify GitHub Pages, and clean up after the release is complete.
